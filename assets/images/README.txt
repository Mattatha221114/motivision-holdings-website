Add your logo file here as: logo.png
Recommended: transparent background PNG or SVG, roughly 300–600px wide.

Optional: add a favicon as: favicon.png (32x32 or 64x64).

Until logo.png is added, the header and footer automatically show a
text-based "MOTIvision" wordmark instead (no broken image icon is shown).
Once you add the real logo file, it will be used automatically —
no code changes are needed.
